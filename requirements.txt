# These libraries are needed to run the app, other libraries (used in development) are found in requirements-dev.txt

streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.0
scikit-learn>=1.3
joblib>=1.3
